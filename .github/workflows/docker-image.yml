name: Docker Image CI

on:
    push:
        branches: ["main"]
        

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@3df4ab11eba7bda6032a0b82a6bb43b11571feac # v4
            - name: docker login
              env:
                  DOCKER_USER: jackbailey
                  DOCKER_PASS: ${{secrets.DOCKERHUB_ACCESS_TOKEN}}
              run: |
                  docker login -u $DOCKER_USER -p $DOCKER_PASS
            - name: Build the Docker image
              run: docker build . --file Dockerfile --tag jackbailey/nextcloud:fpm-ffmpeg
            - name: Build the Docker image
              run: docker build . --file Dockerfile-unRAID --tag jackbailey/nextcloud:unraid
            - name: Docker Push
              run: docker push jackbailey/nextcloud --all-tags
